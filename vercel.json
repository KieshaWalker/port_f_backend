{
    "version": 2,
    "builds": [
        {
            "src": "Portfolio/wsgi.py",
            "use": "@vercel/python",
            "config": {
                "maxLambdaSize": "15mb"
            }
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "Portfolio/wsgi.py"
        },
        {
            "src": "/admin/(.*)",
            "dest": "Portfolio/wsgi.py"
        }
    ],
    "env": {
        "DJANGO_SETTINGS_MODULE": "Portfolio.settings"
    }
}